{
  "name": "my-portfolio",
  "version": "1.0.0",
  "description": "My personal portfolio website",
  "main": "index.js",
  "scripts": {
    "start": "live-server",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r favicon dist/ && cp site.webmanifest dist/",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "live-server": "^1.2.2"
  }
}