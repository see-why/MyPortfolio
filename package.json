{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && serve dist",
    "start": "serve dist"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "serve": "^14.2.0"
  }
}