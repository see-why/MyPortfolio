{
  "name": "my-portfolio",
  "version": "1.0.0",
  "description": "My personal portfolio website",
  "main": "index.js",
  "scripts": {
    "start": "live-server",
    "build": "mkdir -p dist && node build.js && cp -r favicon dist/ && cp site.webmanifest dist/ ",
    "deploy": "gh-pages -d dist",
    "dev": "npm run build && live-server dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "hint": "^6.2.0",
    "live-server": "^1.2.2",
    "stylelint": "^16.20.0",
    "stylelint-config-standard": "^38.0.0",
    "stylelint-csstree-validator": "^3.0.0",
    "stylelint-scss": "^6.12.1"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
